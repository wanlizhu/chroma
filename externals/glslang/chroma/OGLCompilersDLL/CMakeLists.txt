SET(SOURCES ${GLSLANG_DIR}/OGLCompilersDLL/InitializeDll.cpp ${GLSLANG_DIR}/OGLCompilersDLL/InitializeDll.h)

ADD_LIBRARY(OGLCompiler STATIC ${SOURCES})
SET_PROPERTY(TARGET OGLCompiler PROPERTY FOLDER Externals/glslang)
SET_PROPERTY(TARGET OGLCompiler PROPERTY POSITION_INDEPENDENT_CODE ON)

IF(WIN32)
    SOURCE_GROUP("Source" FILES ${SOURCES})
ENDIF()
